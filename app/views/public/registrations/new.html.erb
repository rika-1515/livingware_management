<div class = "container">
  <h5><span class="bg-light text-dark">新規会員登録</h5>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "public/shared/error_messages", resource: resource %>
  
    <table>
      <tr>
        <div class="field">
          <td>
            <%= f.label :ニックネーム %>
          </td>
          <td>
            <%= f.text_field :name, autofocus: true, autocomplete: "name",placeholder:"太郎" %>
            ※ネット上に公開される名前です
          </td>
        </div>
      </tr>
      <tr>
        <div class="field">
          <td>
            <%= f.label :メールアドレス %>
          </td>
          <td>
            <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
          </td>
        </div>
      </tr>
      
      <tr>
        <div class="field">
          <td>
            <%= f.label :パスワード %>
            <% if @minimum_password_length %>
            (<%= @minimum_password_length %> 字以上)
            <% end %>
          </td>
          <td>
            <%= f.password_field :password, autocomplete: "new-password" %>
          </td>
        </div>
      </tr>
      <tr>
        <div class="field">
          <td>
            <%= f.label :パスワード %>(確認用)
          </td>
          <td>
            <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
          </td>
        </div>
      </tr>
    </table>
  
    <div class="actions">
      <%= f.submit "新規会員登録",class:"btn btn-success" %>
    </div>
  <% end %>

  <h5><span class="bg-light text-dark">既に登録済みの方</h5>
  <%=link_to "こちら",new_customer_session_path %>　からログインしてください。

</div>
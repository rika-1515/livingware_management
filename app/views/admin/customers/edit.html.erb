<div class = "container">
    <h5><span class="bg-light text-dark"><%=@customer.name %>さんの会員情報編集</span></h5>
    <% if @list.errors.any? %>
      <%= @list.errors.count %>件のエラーが発生しました
      <ul>
        <% @list.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
      </ul>
    <% end %>

    <%= form_with model: @customer,url:admin_customer_path do |t| %>
    <table>
      <tr>
        <td>会員ID</td>
        <td><%=@customer.id %></td>
      </tr>
      <tr>
          <div class="field">
            <td><%= f.label :ニックネーム %></td>
            <td>
            <%= t.text_field :name, autofocus: true, autocomplete: "name",placeholder:"太郎" %>
            </td>
          </div>
      </tr>
      <tr>
          <div class="field">
            <td>
            <%= t.label :メールアドレス %>
            </td>
            <td>
            <%= t.email_field :email, autofocus: true, autocomplete: "email" %>
            </td>
          </div>
      </tr>
      <tr>
        <td>
          <label for="inputIs_active">会員ステータス</label>
        </td>
        <td>
          <%= t.radio_button :is_active, :true, checked: true %>
          <%= t.label :is_active, "有効", {value: :true, style: "display: inline-block;"} %>
          <%= t.radio_button :is_active, :false %>
          <%= t.label :is_active, "退会", {value: :false, style: "display: inline-block;"} %>
        </td>
      </tr>
    </table>
    

  <div class="actions">
    <%= t.submit "変更を保存",class:"btn btn-success" %>
  </div>
    <% end %>

</div>